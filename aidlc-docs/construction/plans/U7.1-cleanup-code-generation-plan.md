# U7.1 — Code Generation Plan: terminar la limpieza de U7

> **Contexto**: U7 (`53d9960`) hizo el refactor correcto (room estándar único, -1300 líneas console/clásico),
> pero la limpieza quedó incompleta. Re-revisión con 3 agentes (2026-06-23): arquitectura LIMPIA
> (cero edición de libs, cero internals privados, cero orquestación casera de audio, turn config impecable);
> faltan: 2 fantasmas que rompen en runtime, 1 race real del dispatch, huérfanas, y stale en docs/comentarios.
> **Tipo**: cleanup/refactor brownfield, sin lógica nueva. **Modo único: room/LiveKit** (no negociable).

## Pre-step — restaurar U7 al working tree
- [x] `git checkout HEAD -- .` (HEAD = U7 `53d9960`); conservar `DIAGNOSTICO-mic.md` (untracked). Verificar `git diff HEAD` vacío y `git status` limpio salvo el untracked.

## ALTA — rompen en runtime / bug real

- [x] **1. Suite de tests rota** (`tests/test_pure.py:16`): importa `vc.tts` (borrado por U7) → ImportError
  en checkout limpio (el editable-install lo enmascaraba; `py_compile` no atrapa imports). Quitar el import
  de `vc.tts` (`clean_for_tts`, `_next_chunk_cut`, `_HARD_PUNCT_CHARS`, `TTSStreamer`) y los tests que lo
  ejercen. Conservar los tests de `vc.session` (`is_reset_command`, `is_visual_command`). Sacar también los
  tests de `is_goodbye` (se borra en #5).
- [x] **2. `tools/say.py`** (`:19`): importa `vc.tts` borrado, todo el script depende de él (era del flujo
  clásico, probaba sync TTS↔orbe que ya no existe). **Borrar el archivo.**
- [x] **3. Race/fantasma del dispatch** (`vcctl.py:372` + `lk/agent.py`): `_start_room` decide "agente ya en
  el room" por `LK_CTL_SOCK.exists()`. Si el worker crashea, el socket queda stale → se saltea el dispatch →
  el agente no entra → Win+Z falla (el bug histórico, reintroducido por el lado del archivo stale).
  **Fix**: reemplazar `LK_CTL_SOCK.exists()` por `_sock_up(str(LK_CTL_SOCK))` (un socket stale no acepta
  `connect()` → cae al else → despacha). **Defensivo**: en `lk/agent.py`, borrar el socket en shutdown
  (`atexit` o handler de señal) para no dejarlo stale.
- [x] **4. Doble vía de dispatch** (`vcctl.py:233` API proactivo + `orb/orb_server.py:161` token
  `RoomConfiguration(agents=[RoomAgentDispatch(...)])`): dos mecanismos simultáneos. **Fuente única**: dejar
  el dispatch proactivo de vcctl (canónico, ya con retry/backoff) y **sacar el `RoomConfiguration` del
  `/token`**. Verificar que el token sigue minteando correcto (round-trip).

## MEDIA — código muerto / robustez

- [x] **5. `is_goodbye`** (`vc/session.py:70-80`) + `GOODBYE_KEYWORDS` (`vc/config.py:238`): par huérfano,
  cero uso en runtime (no lo cablea `lk/claude_llm.py`). Borrar función, constante y sus 2 tests.
- [x] **6. `WORD_ALIASES_PATH`** (`vc/config.py:206-207`): su consumidor era `clean_for_tts` en el `vc/tts.py`
  borrado. Borrar.
- [x] **7. `WAKE_MAX_IDLE_TURNS`** (`vc/config.py:244`): cero uso (lógica de auto-corte del flujo viejo). Borrar.
- [x] **8. `_node_ip()` loopback silencioso** (`vcctl.py:180-191`): si cae a `127.0.0.1`, el media WebRTC no
  conecta (LiveKit no bindea media a loopback) pero el signaling levanta → HOT falso. Agregar warning explícito
  cuando cae a loopback.

## BAJA — prolijidad (mismo barrido)

- [x] **9. Comentarios/docstrings stale**: `saga.py:5` (sacar `audio/stt/tts` del listado de submódulos);
  `orb/orb_server.py:17,144` ("modo console") + `:5` ("saga.py hace POST /state" → en room lo hace el agente);
  `scripts/train_wakeword.py:16,71` + `scripts/record_wakeword.py` (`agent.py console` → `start`); comentarios
  "flujo clásico" en `lk/agent.py:11,125,147` y `lk/claude_llm.py:65,101`.
- [x] **10. TMP_FILES/config muertos**: `WHISPER_SOCK` (`vc/config.py:22`) + `/tmp/saga-whisper.sock`
  (`vcctl.py:34` TMP_FILES) — `whisper_daemon.py` borrado, el STT fallback hoy corre dentro del agente.
  Verificar antes de quitar que `vc/doctor.py` no rompa. `/tmp/saga.pid|lock|abort|wav`: verificar si
  `vc/app.py`/`saga.py` (vivos, press+doctor) los crean; quitar solo los que ya no se usan. Podar
  `wake_daemon.py` de `DAEMONS` (`vcctl.py:29`) — queda dormido, ya excluido del start.
- [x] **11. Docs `*.md`**: `docs/operations.md`, `architecture.md`, `code-guide.md`, `internal-api.md`,
  `turn-flow.md` — reescribir secciones que describen console/clásico/`VOICE_LIVEKIT`/`SAGA_TRANSPORT` como
  vigentes. `pyproject.toml:23,26,28` comentarios "flujo clásico" → reformular (deps `faster-whisper`/
  `edge-tts`/`sounddevice` SE QUEDAN: fallback local + wake_daemon dormido + doctor). `tech-debt-plan.md`:
  marcar D1/clásico como resuelto, no vigente.

## Criterio resuelto (verificado)
- **`sounddevice`**: NO se borra. La usan `wake_daemon.py` (Vosk dormido) + `vc/doctor.py:58` (health check).
- **`saga.py`**: VIVO (emisor Win+Z `press` + `--doctor` vía `vc/app.py` adelgazado). No se borra; solo docstring.
- **Glue Win+Z** (socket Unix + HTTP): deuda OPCIONAL (migrable a data channel/RPC nativo). FUERA de este scope.
- **Vosk/`wake_daemon.py`**: queda el archivo (dormido intencional, CLAUDE.md lo declara fuera de scope); solo
  se poda de la lista `DAEMONS` para que `status` no lo muestre como pieza viva.

## Build & Test (verificación del ciclo)
- [x] `py_compile` de todos los `.py`.
- [x] **Suite arreglada VERDE**: `.venv/bin/python -m unittest tests.test_pure`.
- [x] grep refs=0 de lo borrado: `vc.tts`, `is_goodbye`, `GOODBYE_KEYWORDS`, `WORD_ALIASES_PATH`,
  `WAKE_MAX_IDLE_TURNS`, `console`, `whisper_daemon`, `RoomConfiguration` (en orb_server).
- [x] import smoke de módulos vivos (`vc.app`, `vc.session`, `lk.agent`, `lk.wakeword`, `lk.claude_llm`,
  `vc.config`, `orb.orb_server`).
- [x] `saga-ctl status` room-only OK.
- [ ] **En vivo (usuario)**: `saga-ctl restart` → "agente en el room HOT" → Win+Z + hablar → responde por voz.

## Commit
- [x] Commit **`cf726f7`** `refactor(ciclo4): U7.1 — limpieza U7 + fix dispatch cold-start` (OK del usuario
  2026-06-24). 18 archivos, +178/-298. NO pusheado.

## Resultado (ejecutado 2026-06-23)
Working tree restaurado a U7 + cleanup aplicado. **18 archivos staged, +135/-290 (net -155)**.
Verificación estática: `py_compile` OK · `tests.test_pure` **11/11 VERDE** (antes ImportError) · import smoke
de 8 módulos vivos OK · grep refs=0 de lo borrado. Verificador adversarial: **0 blockers**, cambio de dispatch
confirmado seguro. Hallazgo nuevo flageado (NO en este commit): cadena muerta del flujo clásico en
`vc/runtime.py` (`PID_FILE`/`LOCK_FILE`/`read_recorder_pid`/`read_owner_pid`/`signal_stop`/`signal_cancel`,
cero llamadores externos) + `wake_daemon.py`/Vosk → deuda para un pase aparte (runtime es el hub conservado +
está en el path de cancel vivo). `DIAGNOSTICO-mic.md` dejado untracked (fuera del commit). Pendiente: commit
(OK del usuario) + validación en vivo (Win+Z).
